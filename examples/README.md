# Examples

These examples show what a configured target repo can look like after copying the templates.

The examples are docs-only. They are not intended to run as software projects.

## Available Examples

- `minimal-target-repo/`: a small fake CLI milestone with one issue brief.
